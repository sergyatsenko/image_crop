"use client";

export default function HomePage() {
  return (
    <main className="card">
      <h1>Responsive Image Crop &amp; Focal Point Editor</h1>
      <p>
        This app is intended to run inside Sitecore XM Cloud Page Builder as a Marketplace Custom Field extension.
        Open <code>/custom-field-extension</code> to use the field editor UI.
      </p>
    </main>
  );
}
